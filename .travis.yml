language: objective-c

notifications:
  email:
    on_success: never
    on_failure: change

  irc:
    channels:
      - "chat.freenode.net#atom-sfdc-ba"

script: 'curl -s https://raw.githubusercontent.com/atom/ci/master/build-package.sh | sh'
